# v2.6.3
- Fixed issue when have 2 world and a player joins to the first world will not teleports to the second world.
- Fixed some issues when we using per-world option and the convert is not succeed.
- Added option to fix all entities damage after player teleportation when a firework launched.
- Improved spawn toggle command to work properly.
- Some fixes related to handling void-spawn option.
- Improved gui building, now no need to specify the empty (air) elements.
- Added option to remember toggled spawns.
- Fixed error when the gui size is higher than 54.

# v2.6.2
- Fixed issue when you have a spawn in a world (nether) and you want to teleport from another world will not became. This contains recodes of handling spawns (again).
- Optimized code when there are a lot of double/float number parsing.
- Added release downloader to download plugin updates.

# v2.6.1
This release contains that fix where the users set a spawn and reload the spawns are set to default (0, 0, 0) values.

+ some improvements for sending error message to operator players 

# v2.6
- Now the players SHOULD pay before using spawn command, to do not ignore the pay.
- Lots of improvements, including code clean up, readability and so on.
- Recoded the spawn handling to avoid any issues when saving or deleting.
- Recoded the config handling to prevent config loading issues.
- Fixed a lot of exception errors when executing commands.
- Now supports mc 1.15+ versions.
- Fixed when the spawns are not loaded correctly on plugin reload.
- Fixed issue when the user toggled the spawn usage for all players and sent a lot of messages.
- Temporary fixed an issue when teleporting with rideable entities and getting damaged because of new versions.
- Now if the option "per-world-spawn" is changed then converts the old style of spawns file to the new format for easy and fast work.
- Now in the progress bar will displays the current time of teleportation. (It still needs some work, but it works.)

# v2.5
Added:
  Added option to teleport the player to the world spawn location if the player executed.
  Added option to firework to explode instantly.
  Added option to prevent teleporting to spawn while a player is in minecart.
  Added sub-command argument to spawn to teleports to other world spawn.
  Added option to signs to teleport to other world spawn if the spawn set and the given world exists.
Removed:
  Removed ability to delete the spawn from console.
Other:
  Now players will be paid after using the spawn.
  There is no longer delay when importing spawn from essentials.

# v2.4
Added:
  Added argument for /sts togglespawn all to toggle the spawn for all online players.
Fixes:
  Fixed NPE when in the GUI item name string not find in configuration.
  Fixed actionbar not work correctly in latest versions.
  Fixed colors not work correctly in GUI for Paper softwares.
Removed:
  Removed /sts disable command - It will broke the plugin when disabled
Other:
  Some minor changes and optimizations.
  Toggled spawns are now saved (when plugin disabled) in a file to remain the toggles.

# v2.3
Added:
  Added option to config to send teleport messages repeatedly or just once.
  Added option to teleport to other world and not only in player world when the `toWorld` string is specified in spawns file.
Fixes:
  Fixed error when finding the Essentials plugin, but not find and tried to register the event, but not success.
  Fixed issue with GUI when the lore list is empty, then throws an error.
  Fixed a minor issue when the progress bar is end, then adds one line after the cooldown.
  Fixed issue when the per-world-spawn is enabled and teleported to spawn, it was not teleport in the center of block.
Removed:
  Fully removed 1.7 support. This version is VERY outdated.
Other:
  Now the teleport task will cancel it firstly before executing other functions.
  Plugin updated to 1.14.
  Spawn sign and GUI now works in 1.14.

# v2.2
Added:
  Added option to prevent spawn use when player is in fire.
  Added option to teleport player to spawn when afk status is true.
Fixes:
  Fix NPE when set the spawn cost and the cost not a number.
  Fix NPE when the string not found in the messages file.
  Fixed when the plugin checked the version, for some reason the scanner was not recognized by any version of Java so it caused a problem.
  Fixed import permission tab-complete not work correctly.
Other:
  Now checks the lines of signs in the configuration list to prevent exceptions.
  Some strings are simplified, such as sound or effect, e.g in the sounds the volume or pitch.
  Now the titles can be used as empty strings, so if title empty and the subtitle not, then sends only the subtitle.
  Now checks in the import command the player group from essentials.
  Now the import only removes the old spawn location when the task is success.

# v2.1
Added:
    Added optional option to set the spawn cost in spawns file. (/setspawn [cost])
    Added /sts togglespawn command to turn on/off the /spawn command use.
    Added configurable sounds to player moved, damaged & jump.
    Added bypass permission for sneak & fly when the player teleports and press sneak or starts flight, and cancel task.
    Added spawn-sign.save-sign-data boolean to configuration. - Now saving the signs data to a file when a player placed a sign.
Fixes:
     A lot of NPE fixes that caused the plugin stop.
     Fix NPE when player died and the respawn function enabled but when spawn not found throws a long error.
     Fixed tab-complete not work correctly.
Other:
    Moved some messages from config.yml to messages.yml file.
    Changed the teleport-on-command for simpler. (starts with player or console)
    Now if any string is empty or not found, it will not send a blank message.

# v2.0
Added:
    Added close-gui-when-player-clicked-on-a-item boolean to configuration.
    Added teleport to bed spawn function. - To teleport the player to the player's bed spawn point.
    Added /sts importessentialsspawn [group] command. - To import Essentials spawn to the plugin.
Removed:
    Removed unnecessary information about the player & world (uuid).
Other:
    Now if a player rides on a skeletonhorse, zombiehorse or llama and teleports to the spawn, then the entity remains.

# v1.9
Added:
    Added durability integer of gui. (Not supported the : mark.)
    Added /sts bukkitspawn command. - To teleport to the default world spawn location.
Fixes:
    Improved teleport & progress bar cooldown problems. - There were problems with stopping the scheduler when the player moved and started teleporting again.
    Fixed NullPointerException of registering cooldown task.
Other:
    Updated to Bukkit/Spigot 1.13 version.
    Now if you want add empty place in GUI you can use AIR in item section.
    Now if a player rides on a horse or pig and teleports to the spawn, then the entity remains.
Changed:
    Changed the item-id to item since 1.13 already takes issue id in the GUI.
Removed:
    Removed plugin update permission.

# v1.8
Added:
    Added launch firework functions.
    Added paying functions to move, jump & damage.
    Added enable-gui boolean to configuration. - To enable/disable the gui.
    Added disable spawn command in pvp functions. - To disable spawn command when you attack a player or attack you.
    Added no-damage-time interval. - When you were teleported to the spawn, there is no damage to anything.
Fixes:
    Fixed when the player attacked other player and started spawn, canceled the spawn task.
    Fixed if teleport cooldown is 0, it will send the message, but it should not have (again).
    Fixed if teleport cooldown is 0, not teleport to the spawn with sign.
    Fixed NullPointerException of sign creating message.
Removed:
    Removed enable-message booleans. If the message is empty not send the message.
Other:
    Now the lore of the gui is string list. Easier :)

# v1.7
Added:
    Added support for setthespawn.* permission. - To access for all permissions.
    Added teleport op players boolean to configuration.
    Added version checker to configurations. - If the version number abandoned.
    Added teleport to world spawn if spawn not found boolean to configuration.
    Added progress bar functions.
Fixes:
    Fixed some config loading problems.
    Fixed when the per world was enabled and the sign has everybody line 1 not shown in the sign.
    Fixed did not find the spawn and sent a long error when the respawn boolean was enabled.
    Fixed some minor issues with delspawn & setspawn command.
Removed:
    Removed %max-players% & %online-players% placeholder.
Other:
    Now supports everyone in spawn sign line 1.
    Now the configurations successfully creating it when reload the plugin.

# v1.6
Added:
    Added cancel-teleport damage function.
    Added title player damage function.
    Added spawn sign line 1 to 'everybody' to teleport all online players.
    Added permission for teleport all online players with sign. setthespawn.sign.use.everybody
    Added height integer to void on spawn configuration.
Fixes:
    Fixed when the player first or joined the server, did not teleport to the spawn.
Removed:
    Removed %file%, %x%, %y%, %z%, %yaw%, %pitch% placeholders.

# v1.5
Added:
    Added /sts stopalltask command.
    Added cancel-teleport move & jump functions.
    Added title player jump function.
Fixes:
    Fixed reload the plugin, close players inventory menu. - When reload the plugin, it closed the inventory of the players.
    Fixed player went to sneak mode and did not start teleportation, stopped all the ongoing teleports.
    Fixed player moved, kept was continually verifying whether the player was moved during teleport, so the server was frozen.
Removed:
    Removed cancel-teleport-on-move function.
Changed:
    Changed spawn-sign-color-line0 to sign-color-line0 string.
    Changed spawn-sign-break-msg-enable to sign-break-msg-enable boolean.
    Changed spawn-sign-break-msg to sign-break-msg string.
# v1.4
     Added:
        Added check sneak while teleport function. - Checks during teleportation by pressing the SHIFT button to cancel the teleportation.
        Added start spawn, end spawn & player move title.
        Added spawn-sign enable boolean. - Now you can disable/enable spawn signs.
    Fixes:
        Fixed /spawn command multiple times. - The /spawn command, once started, can not be repeated until the delay has expired.
        Fixed all tasks stopped. - It has stopped all tasks that run on the server only.
    High fixes:
        High fix when you used the /spawn command several times and had been teleported, the server could be slow and frozen.
    Removed:
        Removed command wait time.
# v1.3
     Added:
        Added disabled worlds. - Now the worlds can be disabled.
        Added check-fly-before-teleport boolean & message. - Now flight mode can not be teleported.
        Added spawn command & sign cost permission.
    Fixes:
        Fixed when reloading the plugin, it deletes the currently executed /spawn command delay.
    Changed:
        Changed per world spawn location set to x, y, z locations.
# v1.2
     Added:
        Added spawn sign. - Now you can spawn on the sign.
        Added multiple permissions.
    Changed:
        Changed main-info-stscommand-on-default to default-can-see-plugin-information boolean.
        Changed enable-message to gui-message-enable boolean.
    Fixes:
        Fixed teleport cooldown message it also writes 0. - When a player teleports and teleport cooldown is set to 0, also writes the message.
        Fixed Vault registering & unregistering.
        Fixed per world spawn it did not work and makes a mistake.
        Fixed cancel teleport. - Did not delete the teleport delay, so the server was lagged.
        Fixed plugin not starting.
        Fixed file creation problems.
    Removed:
        Removed some spaces & code.
# v1.1
     Added:
        Added per world spawn. - In separate worlds you can create spawn locations.
        Added Vault support. - The /spawn command can now be a cost.
        Added player respawn boolean.
    Fixes:
        Fixed plugin not starting.
        Fixed file creation problems.
        Fixed teleport cooldown remove. - The countdown did not stop.
        Fixed icon menu not support colors.
        Fixed player leave event. - When the player left the server during teleporting, he did not delete the delay.
        Fixed /spawn all command there was an error in the console. - When you typed /spawn all and did not find the spawn, you wrote an error in the console.
        Fixed spawn teleport errors.
    Removed:
        Removed auto refresh. - It was unnecessary.
